import { AlignItemCenterDirective } from './align-item-center.directive';

describe('AlignItemCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignItemCenterDirective();
    expect(directive).toBeTruthy();
  });
});
